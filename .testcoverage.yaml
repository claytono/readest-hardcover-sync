profile: coverage.out

threshold:
  total: 95
  file: 90

override:
  # Error paths in StartServer (Load/Listen/Serve) can't be triggered in tests
  - path: ^internal/demo/demo\.go$
    threshold: 85

exclude:
  paths:
    - ^cmd/
